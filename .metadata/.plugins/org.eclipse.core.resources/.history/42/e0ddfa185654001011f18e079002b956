package tests;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.NullAndEmptySource;
import org.junit.jupiter.params.provider.ValueSource;

import domain.Captcha;

class CaptchaTest {
	private Captcha c;

	private static String DEFAULT_CONTENT = "1122";

	@BeforeEach
	void setUp() throws Exception {
		c = new Captcha(DEFAULT_CONTENT);
	}

	/*
	 * CONSTRUCTORS
	 */

	@Test
	void testCaptcha_ValidContent() {
		assertEquals(DEFAULT_CONTENT, c.getContent());
	}

	@ParameterizedTest
	@NullAndEmptySource
	@ValueSource(strings = { "11a22" })
	void testCaptcha_InvalidContent_ThrowsException(String content) {

	}

}
